# Reconstructed Exercise

The original handout is unavailable. Based on the submitted code, the session required students to:

1. interface a 16×2 character LCD with an STM32 in four-bit mode;
2. display identifying text on both LCD rows; and
3. scan a matrix keypad through GPIO and show detected numeric keys on the LCD.

This description is inferred from the surviving source and is not a verbatim assignment statement.
