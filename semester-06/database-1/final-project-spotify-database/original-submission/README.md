# Original submission note

The original course ZIP is intentionally **not committed to this public repository** because the submitted Python file contained a hard-coded local PostgreSQL password.

The repository instead preserves a curated copy of the submitted source code, SQL schema files, ER diagram, and assignment brief. The only security-related edit made to the browsable Python source is replacing the embedded database connection credential with environment-variable configuration.

The private original submission remains outside the public Git repository.
