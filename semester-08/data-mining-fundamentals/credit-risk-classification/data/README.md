# Dataset Status

The submitted notebook expects the following local file:

```text
credit_risk_dataset.csv
```

The CSV was not present in the recovered project archive and is no longer available from the project author. The notebook's embedded outputs show that the original file contained 32,581 rows and 12 columns.

No replacement dataset is included because the exact source, version, and license could not be verified. Consequently, the archived notebook preserves the historical analysis and outputs but cannot be executed end to end without a compatible dataset.
