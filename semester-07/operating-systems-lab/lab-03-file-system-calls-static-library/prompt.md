# Assignment summary

Implement a C command-line utility using file-related system calls and a static library. Required operations included creating files with octal permissions, checking access, displaying file metadata, and creating indexed files in a directory. The library had to be built as a static archive and linked through a Makefile with explicit dependencies.

The original Persian assignment PDF is preserved in `reference-material/`.
