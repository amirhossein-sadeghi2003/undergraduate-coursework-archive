# Assignment summary

The first lab introduced Linux command-line administration and observation tasks. Students worked with root-directory permissions and file metadata, created and modified files, changed permissions and timestamps, used SSH/SCP between lab machines, inspected memory-intensive and `kworker` processes, searched `/etc` with `grep`, and modified the system Bash configuration.

The original Persian assignment PDF is preserved in `reference-material/`.
