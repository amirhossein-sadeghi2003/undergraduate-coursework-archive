# Assignment summary

The original assignment brief was not available during archival. From the submitted files, the lab extended a Linux character device with `ioctl` commands for mute/unmute and volume control, dynamic allocation for reads, user/kernel copying, a user-space test program, and build/teardown scripts.

This summary is inferred from the submission and should not be treated as the exact historical prompt.
