# Assignment summary

1. Compute the dot product of two arrays with multiple POSIX threads while protecting the shared result from concurrent updates.
2. Implement the readers–writers synchronization problem with multiple processes, shared memory, and process-shared synchronization: multiple readers may read concurrently, while writers require exclusive access.

The original Persian assignment PDF is preserved in `reference-material/`.
