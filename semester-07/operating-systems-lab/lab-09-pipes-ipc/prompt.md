# Assignment summary

1. Compute factorial values using multiple child processes and a single unnamed pipe: each child computes a partial product and the parent combines the results.
2. Implement a temperature producer/server exercise using one named pipe (FIFO), sending a `pid` and temperature value and returning the minimum temperature seen so far.

The original Persian assignment PDF is preserved in `reference-material/`.
