# Assignment summary

1. Write a Bash script that accepts a directory, filename prefix, and a list of indices; creates a random number of files and then removes selected files.
2. Build a multi-file C prime-checking program with a header and a Makefile providing `all`, `install`, `uninstall`, and `clean` targets.

The original Persian assignment PDF is preserved in `reference-material/`.
