# Assignment summary

The provided brief asks for a custom Linux system call that receives a PID and returns process/PCB-related information such as process name, state, and memory usage to user space, with an error for an unknown PID. A user-space program should invoke the syscall and display the returned information.

**Provenance warning:** the supplied PDF is internally labeled “Session 8” and “Fall 1401,” even though it was provided with this Lab 07 submission and its content matches the submitted custom-system-call code. The original PDF is retained unchanged in `reference-material/`.
